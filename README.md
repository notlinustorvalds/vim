# Rich's vim configuration
Note the special `.git/config`:

```
[core]
    repositoryformatversion = 0
    filemode = true
    bare = false
    logallrefupdates = true
    ignorecase = true
    eol=lf
    symlinks=true
    editor = vim
```

